require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

describe SquatDataPointsController do

  # This should return the minimal set of attributes required to create a valid
  # SquatDataPoint. As you add validations to SquatDataPoint, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) { { "user_id" => "1" } }

  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # SquatDataPointsController. Be sure to keep this updated too.
  let(:valid_session) { {} }

  describe "GET index" do
    it "assigns all squat_data_points as @squat_data_points" do
      squat_data_point = SquatDataPoint.create! valid_attributes
      get :index, {}, valid_session
      assigns(:squat_data_points).should eq([squat_data_point])
    end
  end

  describe "GET show" do
    it "assigns the requested squat_data_point as @squat_data_point" do
      squat_data_point = SquatDataPoint.create! valid_attributes
      get :show, {:id => squat_data_point.to_param}, valid_session
      assigns(:squat_data_point).should eq(squat_data_point)
    end
  end

  describe "GET new" do
    it "assigns a new squat_data_point as @squat_data_point" do
      get :new, {}, valid_session
      assigns(:squat_data_point).should be_a_new(SquatDataPoint)
    end
  end

  describe "GET edit" do
    it "assigns the requested squat_data_point as @squat_data_point" do
      squat_data_point = SquatDataPoint.create! valid_attributes
      get :edit, {:id => squat_data_point.to_param}, valid_session
      assigns(:squat_data_point).should eq(squat_data_point)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "creates a new SquatDataPoint" do
        expect {
          post :create, {:squat_data_point => valid_attributes}, valid_session
        }.to change(SquatDataPoint, :count).by(1)
      end

      it "assigns a newly created squat_data_point as @squat_data_point" do
        post :create, {:squat_data_point => valid_attributes}, valid_session
        assigns(:squat_data_point).should be_a(SquatDataPoint)
        assigns(:squat_data_point).should be_persisted
      end

      it "redirects to the created squat_data_point" do
        post :create, {:squat_data_point => valid_attributes}, valid_session
        response.should redirect_to(SquatDataPoint.last)
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved squat_data_point as @squat_data_point" do
        # Trigger the behavior that occurs when invalid params are submitted
        SquatDataPoint.any_instance.stub(:save).and_return(false)
        post :create, {:squat_data_point => { "user_id" => "invalid value" }}, valid_session
        assigns(:squat_data_point).should be_a_new(SquatDataPoint)
      end

      it "re-renders the 'new' template" do
        # Trigger the behavior that occurs when invalid params are submitted
        SquatDataPoint.any_instance.stub(:save).and_return(false)
        post :create, {:squat_data_point => { "user_id" => "invalid value" }}, valid_session
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested squat_data_point" do
        squat_data_point = SquatDataPoint.create! valid_attributes
        # Assuming there are no other squat_data_points in the database, this
        # specifies that the SquatDataPoint created on the previous line
        # receives the :update_attributes message with whatever params are
        # submitted in the request.
        SquatDataPoint.any_instance.should_receive(:update_attributes).with({ "user_id" => "1" })
        put :update, {:id => squat_data_point.to_param, :squat_data_point => { "user_id" => "1" }}, valid_session
      end

      it "assigns the requested squat_data_point as @squat_data_point" do
        squat_data_point = SquatDataPoint.create! valid_attributes
        put :update, {:id => squat_data_point.to_param, :squat_data_point => valid_attributes}, valid_session
        assigns(:squat_data_point).should eq(squat_data_point)
      end

      it "redirects to the squat_data_point" do
        squat_data_point = SquatDataPoint.create! valid_attributes
        put :update, {:id => squat_data_point.to_param, :squat_data_point => valid_attributes}, valid_session
        response.should redirect_to(squat_data_point)
      end
    end

    describe "with invalid params" do
      it "assigns the squat_data_point as @squat_data_point" do
        squat_data_point = SquatDataPoint.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        SquatDataPoint.any_instance.stub(:save).and_return(false)
        put :update, {:id => squat_data_point.to_param, :squat_data_point => { "user_id" => "invalid value" }}, valid_session
        assigns(:squat_data_point).should eq(squat_data_point)
      end

      it "re-renders the 'edit' template" do
        squat_data_point = SquatDataPoint.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        SquatDataPoint.any_instance.stub(:save).and_return(false)
        put :update, {:id => squat_data_point.to_param, :squat_data_point => { "user_id" => "invalid value" }}, valid_session
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested squat_data_point" do
      squat_data_point = SquatDataPoint.create! valid_attributes
      expect {
        delete :destroy, {:id => squat_data_point.to_param}, valid_session
      }.to change(SquatDataPoint, :count).by(-1)
    end

    it "redirects to the squat_data_points list" do
      squat_data_point = SquatDataPoint.create! valid_attributes
      delete :destroy, {:id => squat_data_point.to_param}, valid_session
      response.should redirect_to(squat_data_points_url)
    end
  end

end

<%= form_for(@deadlift_data_point) do |f| %>
  <% if @deadlift_data_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@deadlift_data_point.errors.count, "error") %> prohibited this deadlift_data_point from being saved:</h2>

      <ul>
      <% @deadlift_data_point.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :user_id, {:value => current_user.id} %>
    <%= f.hidden_field :date, {:value => (Time.now).strftime("%Y-%m-%d")} %>
  </div>
  <div class="field">
    <%= f.label :data_point %><br />
    <%= f.number_field :data_point %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

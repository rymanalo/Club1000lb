<%= form_for(@squat_data_point) do |f| %>
  <% if @squat_data_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@squat_data_point.errors.count, "error") %> prohibited this squat_data_point from being saved:</h2>

      <ul>
      <% @squat_data_point.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :user_id, {:value => current_user.id} %>
    <%= f.hidden_field :date, {:value => (Time.now).strftime("%Y-%m-%d")} %>
  </div>
  <div class="field">
    <%= f.label :weight %><br />
    <%= f.number_field :weight %>
  </div>
  <div class="actions">
    <button class="btn btn-success">Submit</button>
  </div>
<% end %>